import { BinaryBlob } from './types';
export declare class Principal {
    private _blob;
    static anonymous(): Principal;
    static selfAuthenticating(publicKey: BinaryBlob): Principal;
    static from(other: unknown): Principal;
    static fromHex(hex: string): Principal;
    static fromText(text: string): Principal;
    static fromBlob(blob: BinaryBlob): Principal;
    readonly _isPrincipal = true;
    protected constructor(_blob: BinaryBlob);
    isAnonymous(): boolean;
    toBlob(): BinaryBlob;
    toHash(): BinaryBlob;
    toHex(): string;
    toText(): string;
    toString(): string;
}
